Instrument Drivers
==================

Each instrument comes with specific methods. However, calling the instrument directly should call the most useful of these methods.
For example, directly calling a dmm will return the readout of the dmm. 

.. toctree::

    rohdeschwarz
    agilent
    yokogawa
    standfordresearch
    lecroy
